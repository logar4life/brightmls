services:
  - type: web
    name: brightmls-scraper
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y chromium
      pip install -r requirements.txt
    startCommand: python brightmls.py
    envVars:
      - key: CHROME_BIN
        value: /usr/bin/chromium
